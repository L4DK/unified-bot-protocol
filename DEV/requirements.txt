# filepath: requirements.txt
# project: Unified Bot Protocol (UBP) - Reference Implementation
# component: Umbrella (ALL runtime dependencies across Orchestrator, Bot, Adapters, Integrations)
# author: Michael Landbo (Founder & BDFL of UBP)
#
# license: Apache-2.0
# version: 1.0.0
# last-edit: 2025-09-19
#
# notes:
# - This umbrella file aggregates all runtime deps for all components (excludes dev/test tools).
# - Pin using compatible-release operator (~=) to minimize breakage while allowing patch/minor updates.
# - If you deploy a subset (e.g., only Orchestrator), prefer the component-specific requirements files.

# --- Core Web Framework ---
fastapi
uvicorn[standard]
# 'uvloop' og 'websockets' er inkluderet i uvicorn[standard], men vi beholder dem
# her for at være eksplicitte omkring afhængighederne.
uvloop
httpx

# --- Communication / Transports ---
websockets
grpcio
aiohttp

# --- Data & Storage ---
# 'redis' library now handles async natively (replaced deprecated aioredis)
redis
pydantic
pydantic-settings

# Database Drivers
SQLAlchemy[asyncio]
asyncpg         # Primary Async PostgreSQL Driver
psycopg2-binary # Sync Driver fallback (good for tools/migrations)
greenlet        # Required for SQLAlchemy async operations

# --- Security / AuthN-Z / Crypto ---
cryptography
PyJWT

# Security / AuthN-Z / Crypto
cryptography
PyJWT

# --- Observability ---
prometheus-client
opentelemetry-sdk
structlog

# --- LLMs ---
openai
anthropic
google-genai

# --- IoT / MQTT ---
paho-mqtt
paho-mqtt-helper

# --- Message Queues ---
aio-pika
