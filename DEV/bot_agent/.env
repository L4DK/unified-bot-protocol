# FilePath: "/DEV/bot_agent/.env"
# Description: Configuration for the Bot Agent.
# Author: "Michael Landbo"

# Identity
BOT_ID=bot-01-test
# Capabilities (comma-separated)
CAPABILITIES=message.send,sensor.read,task.execute

# Connection
# Point to your Orchestrator URL (without /ws/...)
ORCHESTRATOR_URL=ws://localhost:8000

# Security
# A unique token you generate (doesn't necessarily match the server in this demo setup,
# unless you have pre-provisioned it in the DB, but the server generates an API key for us)
INITIAL_TOKEN=my-secret-onboarding-token-123

# System
LOG_LEVEL=INFO
